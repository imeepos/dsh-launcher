# 产品宪章:AI 时代的第一步(2026-09-03 全盘重构)

> 用户原话(产品宗旨,勿在后续迭代中漂移):
> 1. AI 时代阻挡普通人使用 AI 的是第一步——安装配置 AI 工具以及需要的基础环境。
> 2. 只要能成功安装 AI 工具,剩下的工作 AI 基本都可以解决;本软件解决的就是第一步的问题。
> 3. 内置 AI 引擎全自动检查宿主电脑环境,帮助用户安装管理多种基础环境、命令行工具等;
>    防止 AI 自己把自己搞崩溃后不能重装、卸载、备份的尴尬场景。

## 1. 定位

**AI 工具的「装机引擎 + 环境管家」**:普通人可用的桌面应用,一键把 AI 工具和它的全部
基础环境(Node/Python/Git/…)装好、配好、验好;AI 引擎自动体检与排障;一切安装托管化,
保证**可卸载、可重装、可备份恢复**。

- 用户:想用 AI 但卡在安装配置的普通人(非开发者优先)。
- 不是什么:不做 AI 工具的功能替代(装完即交接),不做通用文件备份,不做应用商店。
- 交付闭环:`体检 → AI 计划 → 确认 → 托管安装 → 复检 → 交接`;异常时 `快照恢复/重装/卸载`。

## 2. 三大保命能力(与愿景第 3 条一一对应)

| 能力 | 机制 |
|---|---|
| 搞不坏 | 托管隔离:一切由本软件安装的东西进 `~/.dsh-launcher/managed/<组件>/<版本>` 槽位,
  经 shims 目录暴露命令,不散装进系统;AI 每个变更动作前强制自动快照,复检失败自动回滚 |
| 能卸载 | 卸载 = 删槽位 + 删 shims + 还原 shell rc 改动(env_edges 全程留痕) |
| 能重装/备份 | 安装配方(components/versions)与快照(snapshots)持久化;重装=同配方重放;
  备份=managed_root + shell rc 归档,一键恢复 |

## 3. 核心闭环

```text
[体检] probe(内置探针:已装组件/版本/PATH/磁盘/网络/镜像可达)
   ↓
[AI 计划] 引擎按目标(如「装好 dsh」)生成步骤:缺啥依赖 → 装什么版本 → 改哪些配置
   ↓
[确认] auto_apply 关闭时逐计划确认;开启时直接执行
   ↓
[执行] 逐步 apply,变更前自动快照,失败即停
   ↓
[复检] 重跑相关探针;失败 → 自动回滚该计划(restore 快照)
   ↓
[交接] 报告装好了什么、命令怎么用;之后的使用交给用户与 AI 工具本身
```

## 4. 组件目录(内置知识库)

- 基础运行时:Node.js(LTS/当前)、Python、Git
- 包管理器/npm 生态随组件配方内嵌(npm 随 Node;pip 随 Python)
- AI 工具:dsh(DeepSeek Harness)、Claude Code、Codex CLI、Gemini CLI 等,配方含
  依赖声明(如 dsh → node ≥20)与验证命令
- 配方来源:内置目录为主;release-platform 作为 `platform` 来源之一(既有的目录安装流
  与制品校验代码在此复用)

## 5. AI 引擎

内置于应用内的代理循环:读体检结果 → 产出计划(agent_tasks.plan_json)→ 逐步执行
(agent_actions)→ 复检 → 报告。LLM 路由可配(engine_config:provider/model/base_url,
密钥仅存 .env 引用名,不入库)。危险动作(改 rc/删文件/网络下载)全部走动作管道留痕。

## 6. 文档与既有实现的处置

- DESIGN-TOOLS.md / DESIGN-UI.md 作废删除(git 历史可查),定位以本文档为准。
- 既有 TR1 实现处置:目录安装流(rp 连接/制品校验/下载)收编为 `platform` 来源;
  process 相关代码待 UR 排期重审;首跑向导概念保留并升级为主闭环的引导壳。
- 数据模型 v2 见 DESIGN-DB.md;UI 在 DB 确认后重新设计。
