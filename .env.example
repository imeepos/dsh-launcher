# dsh-launcher 环境变量兜底模板,只含占位值,不含真实配置。
# 新 worktree 检出缺 .env 时,post-checkout 钩子会用本文件兜底;
# 请将占位值替换为真实配置。真实 .env 不入库(.gitignore 已拦截)。

OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
